{
    "builds": [{
        "src": "business_stack/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "business_stack/wsgi.py"
        }
    ]
}